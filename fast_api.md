# Tutorial 実行ログ
FastAPI 用の環境構築が終了し、 `main.py` を作成した後の実行ログ

## 1. スクリプトの実行

スクリプトは以下のコマンドで実行する  
`uv run` を使うことで、 activate していなくても `backend/.venv` の環境を使って FastAPI を実行できる

```shell
uv run fastapi dev main.py
```