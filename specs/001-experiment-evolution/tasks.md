# Tasks: Mondel Initial MVP

**Input**: Design documents from `specs/001-experiment-evolution/`

**Prerequisites**: `plan.md`, `spec.md`, `research.md`, `data-model.md`, `contracts/openapi.yaml`, `quickstart.md`, and `.specify/memory/constitution.md`

**Tests**: Required. Constitution Principle IV, Test-First Verification, requires each behavior-changing implementation task to be preceded by a focused test task that is run red before its implementation begins. Automated tests remain in the repository as regression tests after they turn green; temporary diagnostics must be identified separately.

**Task notation**: Every task names one primary responsibility, its target file or files, corresponding user story, prerequisite task IDs, and an executable or observable verification. Lists of related entities, fields, states, or test cases define the acceptance coverage of that named responsibility; they are not separate implementation responsibilities. `P` means the task can run in parallel after its stated dependencies are complete and does not modify the same files as the other parallel tasks.

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Establish reproducible Python, Node.js, test-database, and configuration entry points for every user story.

- [x] T001 Configure FastAPI runtime, database, migration, MLflow, pytest, and lint dependencies in `backend/pyproject.toml` and lock them in `backend/uv.lock`; 対応: 共通 (US1-US4); 依存: なし; 検証: `cd backend && uv sync --all-groups` completes and exposes `pytest`, `alembic`, `sqlalchemy`, `pymysql`, `mlflow`, and `ruff`.
- [x] T002 Create the minimal React/Vite/TypeScript application shell and dependencies, including Vitest as a development dependency, in `frontend/package.json`, `frontend/package-lock.json`, `frontend/vite.config.ts`, `frontend/tsconfig.json`, `frontend/index.html`, `frontend/src/main.tsx`, and `frontend/src/App.tsx`; define `scripts.test` as `vitest` and configure the Vite development server to proxy `/api` to `http://127.0.0.1:8000`; 対応: 共通 (US1-US4); 依存: なし; 検証: `cd frontend && npm ci && npm run build` completes with the minimal application entry point, `cd frontend && npm pkg get scripts.test` reports `"vitest"`, and `frontend/vite.config.ts` contains the exact `/api` proxy target; subsequent verification commands use `npm test -- --run` to run once without watch mode.
- [x] T003 [P] Define non-secret backend configuration names and sample values in `backend/.env.example`; 対応: 共通 (US1-US4); 依存: なし; 検証: the file documents `MONDEL_DATABASE_URL`, `MONDEL_TEST_DATABASE_URL`, and `MLFLOW_TRACKING_URI` without embedding credentials.
- [x] T004 [P] Provision the dedicated MySQL 8 test service with a health check in `./docker-compose.test.yml`; 対応: 共通 (US1-US4); 依存: なし; 検証: `docker compose -f docker-compose.test.yml up -d` reports the database healthy and accepts the configured test connection.
- [x] T005 Configure backend test discovery and test-only environment selection in `backend/pytest.ini`, and add a test-environment smoke test in `backend/tests/unit/test_test_environment.py`; 対応: 共通 (US1-US4); 依存: T001, T003, T004; 検証: `cd backend && uv run pytest tests/unit/test_test_environment.py` passes and confirms tests use `MONDEL_TEST_DATABASE_URL` rather than a production database URL.
- [x] T006 Configure Vitest, React Testing Library setup, and the browser-like test environment in `frontend/vitest.config.ts` and `frontend/src/test/setup.ts`, and add a rendering smoke test in `frontend/src/test/setup.test.tsx`; 対応: 共通 (US1-US4); 依存: T002; 検証: `cd frontend && npm test -- --run src/test/setup.test.tsx` passes and confirms a component can render in the test environment.
- [x] T007 Document the reproducible backend, frontend, and test-database bootstrap commands and the local `Vite /api -> FastAPI http://127.0.0.1:8000` request path in `./README.md`; 対応: 共通 (US1-US4); 依存: T001-T006; 検証: the commands in `./README.md` match the scripts and environment variable names created in T001-T006 and explain that browser code uses relative `/api/v1` URLs without FastAPI CORS.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Build the shared persistence, API, error, and HTTP-client boundaries that all user stories require.

**Critical**: Complete this phase before implementing a user-story increment.

- [x] T008 [P] Write failing configuration tests for required variables, invalid database URLs, and explicit test-database selection in `backend/tests/unit/test_config.py`; include the minimum importable `Settings` / `load_settings` scaffold in `backend/app/__init__.py` and `backend/app/config.py`, plus `pythonpath` in `backend/pytest.ini`, without implementing loading or validation; 対応: 共通 (US1-US4); 依存: T001, T003, T005; 検証: `cd backend && uv run pytest tests/unit/test_config.py` reaches the configuration assertions and fails because validated settings are not implemented. T008 evidence (2026-09-08): 30 cases failed at return-shape / expected-ValueError assertions, not collection or import errors; the existing environment smoke test passed (1 case), and Ruff passed. T009 remains unimplemented.
- [x] T009 Implement validated environment loading and application settings in `backend/app/config.py`; 対応: 共通 (US1-US4); 依存: T008; 検証: `cd backend && uv run pytest tests/unit/test_config.py` passes for required, invalid, and test-database cases. T009 evidence (2026-09-08): the unchanged T008 cases plus the existing environment smoke test passed (31 cases total); Ruff passed. No database/MLflow connection or FastAPI startup integration is included.
- [x] T010 [P] Write failing database Session lifecycle and transaction commit/rollback tests, plus a structural assertion that the one-Session-per-request dependency is a normal function rather than a coroutine function, with the minimum reusable MySQL test connection setup, in `backend/tests/conftest.py` and `backend/tests/integration/test_database.py`; include importable no-behavior interfaces in `backend/app/infrastructure/__init__.py` and `backend/app/infrastructure/database.py` so assertions run before T011; 対応: 共通 (US1-US4); 依存: T004, T005, T009; 検証: `cd backend && uv run --env-file .env.example pytest tests/integration/test_database.py` reaches the lifecycle assertions and fails because the Engine, Session, and transaction helpers are not implemented. T010 evidence (2026-09-10): 8 failed / 2 passed, no setup/import errors; passing cases confirm the dedicated MySQL fixture and synchronous generator structure. Existing unit tests: 82 passed; Ruff passed; no T010 probe tables remained after teardown. Request lifecycle tests currently stop at the missing Session assertion; T011 must reach the cleanup assertions.
- [ ] T011 Implement the synchronous SQLAlchemy engine, normal-`def` one-Session-per-request dependency, and transaction commit/rollback helpers in `backend/app/infrastructure/database.py`; 対応: 共通 (US1-US4); 依存: T010; 検証: `cd backend && uv run --env-file .env.example pytest tests/integration/test_database.py` passes, confirms the request-Session dependency is not a coroutine function, and does not share uncommitted state between tests.
- [ ] T012 [P] Write failing model-metadata and migration integration tests for all normalized tables, indexes, constraints, and the singleton lineage guard in `backend/tests/integration/test_initial_schema.py`; 対応: 共通 (US1-US4); 依存: T010, T011; 検証: `cd backend && uv run pytest tests/integration/test_initial_schema.py` reaches schema assertions and fails because the models and migration are absent.
- [ ] T013 Implement the initial relational model metadata as one cohesive SQLAlchemy persistence boundary in `backend/app/infrastructure/models.py`; its related tables are `evolution_step`, `lineage_mutation_guard`, `run_reference`, `run_snapshot`, `run_parameter`, `best_step_metric`, `dataset_input`, and `evolution_step_history`; 対応: 共通 (US1-US4); 依存: T011, T012; 検証: `cd backend && uv run pytest tests/integration/test_initial_schema.py -k model_metadata` passes for the complete metadata boundary while migration-backed cases remain red.
- [ ] T014 Initialize Alembic configuration and metadata discovery in `backend/alembic.ini` and `backend/migrations/env.py`; 対応: 共通 (US1-US4); 依存: T013; 検証: `cd backend && uv run alembic revision --autogenerate -m smoke --rev-id smoke` detects the model metadata, after which the temporary revision is removed.
- [ ] T015 Create the initial MySQL schema and singleton lineage guard seed in `backend/migrations/versions/001_initial_schema.py`; 対応: 共通 (US1-US4); 依存: T013, T014; 検証: `cd backend && uv run pytest tests/integration/test_initial_schema.py` passes after applying migrations to the dedicated test database.
- [ ] T016 Write failing direct-handler and HTTP contract tests for the API error envelope and trace ID, with the minimum reusable FastAPI TestClient fixture, in `backend/tests/conftest.py` and `backend/tests/contract/test_error_responses.py`; 対応: 共通 (US1-US4); 依存: T005, T009, T010; 検証: after T010 has finished editing the shared fixture file, `cd backend && uv run pytest tests/contract/test_error_responses.py` reaches response assertions and fails because the handlers and their FastAPI registration are absent.
- [ ] T017 [P] Implement structured API exceptions, `code`/`message`/`traceId` responses, and trace-ID logging in `backend/app/api/errors.py`; 対応: 共通 (US1-US4); 依存: T016; 検証: `cd backend && uv run pytest tests/contract/test_error_responses.py -k direct_handler` passes for direct handler serialization while HTTP registration cases remain red until T018.
- [ ] T018 Create the `/api/v1` router registration, request-Session lifecycle, and application-level exception and trace middleware registration in `backend/app/api/router.py` and `backend/main.py`; 対応: 共通 (US1-US4); 依存: T011, T015, T017; 検証: `cd backend && uv run pytest tests/contract/test_error_responses.py` passes and `GET /api/v1/unknown` returns the documented error envelope with a trace ID.
- [ ] T019 [P] Write a failing same-origin typed-fetch test in `frontend/src/api/client.test.ts` covering the relative `/api/v1` base, success, API-error, and network-error behavior; 対応: 共通 (US1-US4); 依存: T006; 検証: before T020, `cd frontend && npm test -- --run src/api/client.test.ts` reaches the URL assertion and fails because the shared client is absent, proving requests do not embed an absolute FastAPI origin.
- [ ] T020 Implement the typed browser `fetch` wrapper with the same-origin relative `/api/v1` base, JSON parsing, and typed API errors in `frontend/src/api/client.ts`; 対応: 共通 (US1-US4); 依存: T019; 検証: `cd frontend && npm test -- --run src/api/client.test.ts` passes for the relative request URL, success, server error, and network failure cases.

**Checkpoint**: Configuration, database transactions, migrations, versioned API errors, and the typed HTTP client each have focused red-to-green evidence. All automated tests remain in the repository as regression tests.

---
## Phase 3: User Story 1 - Evolution Stepを定義してRunを紐付ける (Priority: P1)

**Goal**: Create and edit an Evolution Step, select valid MLflow Runs, preserve change history, and view each linked Run's saved information, all Parameters and Dataset Inputs, and lazily loaded best-step Metrics while synchronizing current Run state.

**Independent Test**: With MLflow test fixtures, create a Step with purpose and hypothesis, optionally choose a parent Run and change description, attach a distinct result Run, then verify the detail page shows saved text, Run reference state, Snapshot state, all captured Parameters and Dataset Input metadata, lazily loaded Metrics from each Run's best-accuracy step, and post-creation history. This must work with only a result Run linked, without implementing the US3 comparison endpoint. Open the separate edit page, confirm saved values are prefilled, change or clear Run links through PATCH, and verify the freshly loaded detail reflects the successful update while a conflict remains actionable.

### Tests for User Story 1 - Write and Run Red First

- [ ] T021 [P] [US1] Write failing domain tests for non-blank fields, nullable change descriptions, signed Metric steps, and canonical terminal Snapshot selection in `backend/tests/unit/domain/test_evolution_step_and_snapshot_rules.py`; 対応: US1; 依存: T008-T020; 検証: before T036-T037, `cd backend && uv run pytest tests/unit/domain/test_evolution_step_and_snapshot_rules.py` fails for the unimplemented rules.
- [ ] T022 [P] [US1] Write failing duplicate-result, equal-parent/result, and multi-generation cycle validation tests in `backend/tests/unit/domain/test_run_link_validation.py`; 対応: US1; 依存: T008-T020; 検証: before T038, `cd backend && uv run pytest tests/unit/domain/test_run_link_validation.py` fails for link validation.
- [ ] T023 [P] [US1] Write failing MLflow candidate-search tests that distinguish Run lifecycle from execution status, exclude deleted-lifecycle Runs, and enforce `start_time DESC, run_id ASC` ordering across same-time page boundaries without duplicates, plus current-Run lookup and terminal/active capture payload tests, in `backend/tests/unit/infrastructure/test_mlflow_run_gateway.py`, with reusable active/deleted lifecycle MLflow fakes in `backend/tests/fakes/mlflow.py`; 対応: US1; 依存: T008-T020; 検証: before T039-T041, `cd backend && uv run pytest tests/unit/infrastructure/test_mlflow_run_gateway.py` fails for the missing gateway, lifecycle filtering, exact tie-break order, page continuation, and adapters.
- [ ] T024 [P] [US1] Write failing Evolution Step repository and create/patch/history integration tests covering no-op updates, link changes, unlinking, successful shared-parent Run fan-out across multiple Steps, `409` result ownership, and exact `(changed_at ASC, id ASC)` history ordering with multiple changed fields sharing one timestamp in `backend/tests/integration/test_evolution_step_mutations.py`; 対応: US1; 依存: T008-T020; 検証: before T042, T044, and T045, `cd backend && uv run pytest tests/integration/test_evolution_step_mutations.py` reaches and fails the missing repository/use-case assertions, including stable same-timestamp history order without exposing the internal ID and a positive case where one Run remains the parent of multiple Steps.
- [ ] T025 [P] [US1] Write a failing Run repository and active-to-terminal synchronization integration test with immutable Snapshot assertions in `backend/tests/integration/test_run_link_and_sync.py`; 対応: US1; 依存: T008-T020; 検証: before T043 and T046, `cd backend && uv run pytest tests/integration/test_run_link_and_sync.py` fails for missing persistence and synchronization behavior.
- [ ] T026 [P] [US1] Write failing OpenAPI contract tests for Run search/synchronization, Evolution Step create/read/patch, validation, conflict, and MLflow `502` responses in `backend/tests/contract/test_runs_and_evolution_steps.py`, including a `schema` test group and structural tests that use `inspect.iscoroutinefunction()` to require normal-`def` Evolution Step and Run search/synchronization path operations; 対応: US1; 依存: T008-T020; 検証: before T048-T053, `cd backend && uv run pytest tests/contract/test_runs_and_evolution_steps.py` reaches response and call-style assertions and fails for absent endpoints.
- [ ] T027 [P] [US1] Write failing reusable create/edit form tests for required fields, prefilled purpose/hypothesis/change description and Run values, change-description clearing to `null`, Run selection, reselection and explicit unlinking, and backend validation feedback in `frontend/src/features/evolution-steps/EvolutionStepForm.test.tsx`; assert each candidate displays `runId`, `runName`, `mlflowExperimentId`, `mlflowExperimentName`, `status`, `startedAt`, and `endedAt` from OpenAPI `RunCandidate`, including explicit unset states for nullable fields, and name the Run-picker group `candidate`; 対応: US1; 依存: T006, T020; 検証: before T057-T059, `cd frontend && npm test -- --run src/features/evolution-steps/EvolutionStepForm.test.tsx` reaches and fails the missing form and candidate-field assertions, including choosing between identical Run names.
- [ ] T028 [P] [US1] Write failing local-detail, pending/captured-Snapshot, ordered-history, and non-blocking-sync-warning tests in `frontend/src/features/evolution-steps/EvolutionStepDetailPage.test.tsx`; assert saved Step text/timestamps, linked Run reference fields, every captured Parameter key/value, and each Dataset Input's `ordinal`, `name`, `digest`, `sourceType`, `source`, `schema`, `profile`, and `context`; cover result-only lazy best-step Metric loading, pending-to-captured reload after successful sync, and retaining Parameters/Datasets when accuracy is missing; group pure display assertions as `rendering` and request-flow assertions separately; 対応: US1; 依存: T006, T020; 検証: before T060-T063, `cd frontend && npm test -- --run src/features/evolution-steps/EvolutionStepDetailPage.test.tsx` reaches and fails the absent display and loading assertions; it must distinguish pending from empty captured data, load Metrics without a comparison request, retain local detail on sync failure, and avoid timer-based polling or a sync/reload loop.

- [ ] T029 [P] [US1] Write a failing typed Evolution Step API-client request and error-mapping test in `frontend/src/api/evolutionSteps.test.ts`; 対応: US1; 依存: T006, T020; 検証: before T054, `cd frontend && npm test -- --run src/api/evolutionSteps.test.ts` fails because the feature-specific API functions are absent.
- [ ] T030 [P] [US1] Write failing typed Run API-client tests in `frontend/src/api/runs.test.ts`, grouping cases as `search`, `sync`, and `best-step-metrics`; verify the search URL/query and opaque page token, sync POST and upstream-error mapping, and best-step GET URL, signed steps, available items, and empty unavailable lists; 対応: US1; 依存: T006, T020; 検証: before T055 and T056, `cd frontend && npm test -- --run src/api/runs.test.ts` reaches request/response assertions and fails for the missing feature-specific functions.
- [ ] T031 [P] [US1] Write a failing create-page submission, conflict-feedback, and successful-navigation test in `frontend/src/pages/EvolutionStepCreatePage.test.tsx`; 対応: US1; 依存: T006, T020; 検証: before T059, `cd frontend && npm test -- --run src/pages/EvolutionStepCreatePage.test.tsx` fails because the create page is absent.

- [ ] T032 [P] [US1] Write a failing edit-page test for loading existing purpose, hypothesis, change description and Run values, sending PATCH text updates and explicit `null` clears, parent/result Run reselection and unlinking, `409` feedback, and detail reload after successful navigation in `frontend/src/pages/EvolutionStepEditPage.test.tsx`; 対応: US1; 依存: T006, T020; 検証: before T064, `cd frontend && npm test -- --run src/pages/EvolutionStepEditPage.test.tsx` fails because the edit page and route are absent.

- [ ] T033 [P] [US1] Write failing persisted best-step Metric read tests in `backend/tests/integration/test_best_step_metrics.py`; cover selected-step-only canonical items, negative steps, pending references, missing accuracy, and unknown local Runs, with an MLflow fake that rejects calls; 対応: US1; 依存: T008-T020; 検証: before T047, `cd backend && uv run pytest tests/integration/test_best_step_metrics.py` reaches the read assertions and fails for the missing local read service, not for unprepared database fixtures.
- [ ] T034 [P] [US1] Write failing BestStepMetrics schema, route-level, and registered-application contract tests in `backend/tests/contract/test_best_step_metrics_contract.py`; cover available/unavailable `200`, local-Run `404`, invalid-ID `422`, signed steps, selected-step items, status/reason/null consistency, and a structural assertion that the best-step Metrics path operation is not a coroutine function; 対応: US1; 依存: T008-T020; 検証: before T049, T052, and T053, `cd backend && uv run pytest tests/contract/test_best_step_metrics_contract.py` reaches and fails the absent contract and call-style assertions using test groups named `schema`, `route`, and `application`.
- [ ] T035 [P] [US1] Write failing available, snapshot-pending, accuracy-missing, and Metric-item rendering tests in `frontend/src/features/evolution-steps/BestStepMetrics.test.tsx`; 対応: US1; 依存: T006, T020; 検証: before T061, `cd frontend && npm test -- --run src/features/evolution-steps/BestStepMetrics.test.tsx` reaches and fails the missing component assertions for each item's name/value/step/recorded time and explicit unavailable reasons.

### Implementation for User Story 1

- [ ] T036 [P] [US1] Implement Evolution Step value objects and purpose/hypothesis/change-description validation in `backend/app/domain/evolution_step.py`; 対応: US1; 依存: T021; 検証: `cd backend && uv run pytest tests/unit/domain/test_evolution_step_and_snapshot_rules.py -k 'blank or change_description'` passes.
- [ ] T037 [P] [US1] Implement Metric canonicalization and immutable Snapshot payload selection in `backend/app/domain/run_snapshot.py`; 対応: US1; 依存: T021; 検証: `cd backend && uv run pytest tests/unit/domain/test_evolution_step_and_snapshot_rules.py -k 'metric or snapshot'` passes, including signed and tied steps.
- [ ] T038 [P] [US1] Implement proposed-link validation and deterministic DFS cycle detection in `backend/app/domain/lineage.py`; 対応: US1; 依存: T022; 検証: `cd backend && uv run pytest tests/unit/domain/test_run_link_validation.py` passes for duplicate, same-Run, and cyclic links.
- [ ] T039 [P] [US1] Define MLflow search/loader gateway protocols and normalized Run, Metric, and Dataset transfer types in `backend/app/infrastructure/mlflow_gateway.py`; 対応: US1; 依存: T023; 検証: gateway unit tests can inject in-memory search and loader fakes without importing the MLflow client.
- [ ] T040 [P] [US1] Implement the MLflow Run-candidate search adapter with active-experiment lookup, explicit `run_view_type=ViewType.ACTIVE_ONLY` Run filtering, case-insensitive filtering, `start_time DESC, run_id ASC` ordering, and opaque paging in `backend/app/infrastructure/mlflow_run_search.py`; 対応: US1; 依存: T023, T039; 検証: `cd backend && uv run pytest tests/unit/infrastructure/test_mlflow_run_gateway.py -k 'candidate or search or paging or lifecycle'` passes with deleted Runs excluded and deterministic, duplicate-free same-time page boundaries.
- [ ] T041 [P] [US1] Implement the MLflow selected-Run loader for current metadata and terminal Snapshot capture data in `backend/app/infrastructure/mlflow_run_loader.py`; 対応: US1; 依存: T023, T037, T039; 検証: `cd backend && uv run pytest tests/unit/infrastructure/test_mlflow_run_gateway.py -k 'lookup or active or terminal or capture'` passes.
- [ ] T042 [P] [US1] Implement Evolution Step, change-history, and lineage-guard persistence in `backend/app/infrastructure/evolution_step_repository.py`, ordering history reads by `(changed_at ASC, id ASC)` while keeping the internal history ID out of API data; 対応: US1; 依存: T013, T015, T024; 検証: `cd backend && uv run pytest tests/integration/test_evolution_step_mutations.py -k repository` passes for current links, explicit `NULL` values, oldest-to-newest history with stable same-timestamp ordering, and guard-row locking.
- [ ] T043 [P] [US1] Implement Run Reference, immutable Snapshot, Parameter, best-step Metric, and Dataset Input persistence in `backend/app/infrastructure/run_repository.py`; 対応: US1; 依存: T013, T015, T025; 検証: `cd backend && uv run pytest tests/integration/test_run_link_and_sync.py -k repository` passes for pending references, atomic capture, and immutable reuse.
- [ ] T044 [US1] Implement the atomic Evolution Step create use case with selected-Run prevalidation, result ownership, same-Run rejection, and cycle checks in `backend/app/services/evolution_step_service.py`; 対応: US1; 依存: T036, T038, T041-T043, T024; 検証: `cd backend && uv run pytest tests/integration/test_evolution_step_mutations.py -k create` passes without initial history rows or partial writes.
- [ ] T045 [US1] Implement the atomic Evolution Step patch use case with no-op detection, Run link/unlink handling, history appends, result ownership, and cycle checks in `backend/app/services/evolution_step_service.py`; 対応: US1; 依存: T044, T024; 検証: `cd backend && uv run pytest tests/integration/test_evolution_step_mutations.py -k 'patch or history or no_op or conflict'` passes without partial writes.
- [ ] T046 [P] [US1] Implement local Run synchronization, active-state retention, one-time terminal Snapshot capture, immutable recapture protection, and MLflow-failure rollback in `backend/app/services/run_sync_service.py`; 対応: US1; 依存: T037, T041, T043, T025; 検証: `cd backend && uv run pytest tests/integration/test_run_link_and_sync.py` passes for active, terminal, repeat-sync, and MLflow-failure cases.
- [ ] T047 [P] [US1] Implement persisted best-step Metric reads and pending/accuracy-missing state mapping in `backend/app/services/best_step_metrics_service.py`; 対応: US1; 依存: T037, T043, T033; 検証: `cd backend && uv run pytest tests/integration/test_best_step_metrics.py` passes using only locally stored canonical Metrics at the selected best-accuracy step, with no MLflow call or comparison-service dependency.
- [ ] T048 [P] [US1] Define create, patch, detail, history, and linked-Run request/response schemas in `backend/app/api/schemas/evolution_steps.py`; 対応: US1; 依存: T026, T045; 検証: schema serialization emits camel-case fields and explicit JSON `null` values required by `contracts/openapi.yaml`.
- [ ] T049 [P] [US1] Define Run search, synchronization, Snapshot-state, and BestStepMetrics response schemas in `backend/app/api/schemas/runs.py`; 対応: US1; 依存: T026, T034, T046, T047; 検証: the schema-serialization cases in `cd backend && uv run pytest tests/contract/test_runs_and_evolution_steps.py tests/contract/test_best_step_metrics_contract.py -k schema` pass for pending/captured states, signed steps, Metric items, and mutually exclusive availability/reason/null combinations; HTTP cases remain red until the routes are registered.
- [ ] T050 [P] [US1] Implement Evolution Step create, read, and patch routes as normal-`def` path operations because they invoke synchronous database/MLflow-backed services, with request validation and error mapping in `backend/app/api/routes/evolution_steps.py`; 対応: US1; 依存: T045, T048; 検証: the Evolution Step portions of `cd backend && uv run pytest tests/contract/test_runs_and_evolution_steps.py` pass, including the non-coroutine structure assertions.
- [ ] T051 [P] [US1] Implement MLflow Run search and synchronization routes as normal-`def` path operations because they invoke the synchronous MLflow client and database services in `backend/app/api/routes/runs.py`; 対応: US1; 依存: T040, T046, T049; 検証: the Run-search and sync portions of `cd backend && uv run pytest tests/contract/test_runs_and_evolution_steps.py` pass, including the non-coroutine structure assertions. The separate best-step Metrics route follows in T052 within US1.
- [ ] T052 [US1] Add normal-`def` GET `/runs/{runId}/best-step-metrics` in `backend/app/api/routes/runs.py` after the search/sync routes because it invokes a synchronous database-backed service and to avoid concurrent edits to that file; 対応: US1; 依存: T034, T049, T051, T047; 検証: route-level cases in `cd backend && uv run pytest tests/contract/test_best_step_metrics_contract.py -k route` pass for the non-coroutine structure assertion, available/unavailable `200`, unknown-local-Run `404`, and invalid-ID `422`; full application registration is verified in T053.
- [ ] T053 [US1] Register Evolution Step and Run routers under `/api/v1` in `backend/app/api/router.py`; 対応: US1; 依存: T018, T050, T051, T052; 検証: `cd backend && uv run pytest tests/contract/test_runs_and_evolution_steps.py tests/contract/test_best_step_metrics_contract.py` passes end to end, including best-step Metric reads without a US3 comparison route.
- [ ] T054 [P] [US1] Add typed Evolution Step create, read, and patch API functions in `frontend/src/api/evolutionSteps.ts`; 対応: US1; 依存: T020, T026, T029; 検証: `cd frontend && npm test -- --run src/api/evolutionSteps.test.ts` passes with the documented URLs, methods, request bodies, and typed conflict errors.
- [ ] T055 [P] [US1] Add typed Run-search and synchronization API functions in `frontend/src/api/runs.ts`; 対応: US1; 依存: T020, T026, T030; 検証: `cd frontend && npm test -- --run src/api/runs.test.ts -t 'search|sync'` passes while retaining opaque page tokens and classifying `502` as an upstream synchronization failure; the `best-step-metrics` group remains red until T056.
- [ ] T056 [US1] Add typed best-step Metric retrieval and response types in `frontend/src/api/runs.ts` after the search/sync functions; 対応: US1; 依存: T030, T055, T052; 検証: `cd frontend && npm test -- --run src/api/runs.test.ts` passes for the correct GET URL, signed `bestAccuracyStep`, Metric items, empty unavailable lists, and existing search/sync behavior.
- [ ] T057 [US1] Implement pageable, searchable MLflow Run selection in `frontend/src/features/evolution-steps/RunPicker.tsx`, displaying Run ID/name, MLflow Experiment ID/name, execution status, and start/end times for every candidate, with an explicit unset display for nullable fields; 対応: US1; 依存: T055, T027; 検証: the candidate cases in `cd frontend && npm test -- --run src/features/evolution-steps/EvolutionStepForm.test.tsx -t candidate` pass for every OpenAPI `RunCandidate` display field, duplicate-name selection by Run ID, and a second candidate page.
- [ ] T058 [US1] Implement the accessible reusable Evolution Step form for create/edit modes with initial text and Run values, local validation, change-description clearing to `null`, parent/result Run selection, reselection and explicit unlinking, and submit feedback in `frontend/src/features/evolution-steps/EvolutionStepForm.tsx`; 対応: US1; 依存: T054, T057, T027; 検証: `cd frontend && npm test -- --run src/features/evolution-steps/EvolutionStepForm.test.tsx` passes for both create and edit modes.
- [ ] T059 [US1] Implement the create-page submission and navigation flow in `frontend/src/pages/EvolutionStepCreatePage.tsx`; 対応: US1; 依存: T031, T058; 検証: `cd frontend && npm test -- --run src/pages/EvolutionStepCreatePage.test.tsx` passes for successful detail navigation and actionable `409` feedback.
- [ ] T060 [US1] Implement the pure saved-detail presentation component in `frontend/src/features/evolution-steps/EvolutionStepDetail.tsx`; its display coverage includes saved Step text/timestamps, linked Run reference and Snapshot states, all captured Parameter key/value pairs, Dataset Input metadata (`ordinal`, `name`, `digest`, `sourceType`, `source`, `schema`, `profile`, `context`), and ordered history; 対応: US1; 依存: T054, T055, T028; 検証: rendering cases in `cd frontend && npm test -- --run src/features/evolution-steps/EvolutionStepDetailPage.test.tsx -t rendering` pass for every required field, pending versus captured-empty data, nullable history values, and visible Parameters/Datasets without accuracy; full Metric history is not rendered.
- [ ] T061 [US1] Implement per-Run best-step Metric status and item rendering in `frontend/src/features/evolution-steps/BestStepMetrics.tsx`; 対応: US1; 依存: T035, T056; 検証: `cd frontend && npm test -- --run src/features/evolution-steps/BestStepMetrics.test.tsx` passes for available, snapshot-pending, accuracy-missing, and Metric-item states using local API data only.
- [ ] T062 [US1] Implement the detail-page entry lifecycle orchestrator in `frontend/src/pages/EvolutionStepDetailPage.tsx`; within that single lifecycle, show saved local detail first, synchronize each linked Run at most once, reload detail and best-step Metrics after successful synchronization, load per-Run best-step Metrics lazily, and retain the local view with a non-blocking warning on failure; 対応: US1; 依存: T055, T060, T056, T061, T028; 検証: `cd frontend && npm test -- --run src/features/evolution-steps/EvolutionStepDetailPage.test.tsx` passes with a result-only Run and no comparison endpoint, displays best-step Metrics after pending-to-captured sync, and preserves local detail on failure; neither elapsed time nor the post-sync reload starts a new sync loop.
- [ ] T063 [US1] Add create and detail navigation routes and connect the application bootstrap in `frontend/src/routes/AppRoutes.tsx` and `frontend/src/main.tsx`; 対応: US1; 依存: T059, T062; 検証: `cd frontend && npm run build` completes and direct navigation to create and detail routes resolves. The list route is added with the US2 list page in T079.
- [ ] T064 [US1] Implement the detail-page edit entry point and React Router `/evolution-steps/:evolutionStepId/edit` route, load saved purpose, hypothesis, change description and Run values, reuse the form, submit PATCH updates and explicit `null` clears, retain actionable `409` feedback, and navigate back to a freshly loaded detail in `frontend/src/pages/EvolutionStepEditPage.tsx`, `frontend/src/features/evolution-steps/EvolutionStepDetail.tsx`, and `frontend/src/routes/AppRoutes.tsx`; 対応: US1; 依存: T032, T053-T063; 検証: `cd frontend && npm test -- --run src/features/evolution-steps/EvolutionStepForm.test.tsx src/pages/EvolutionStepEditPage.test.tsx src/features/evolution-steps/EvolutionStepDetailPage.test.tsx` passes for initial values, update/unlink requests, conflicts, and post-update detail reload.

**Checkpoint**: US1 is independently demonstrable with MLflow fakes and has red-to-green unit, integration, contract, and component-test evidence. US1 includes all captured Parameters and Dataset Inputs plus a complete local best-step Metrics API/client/component path; result-only viewing must pass before any US3 comparison work.

---

## Phase 4: User Story 2 - Evolution Stepの全体像を確認する (Priority: P2)

**Goal**: Show a stable, token-paginated Evolution Step list with comparison summaries and preserve detail readability during synchronization failures.

**Independent Test**: Seed more than 20 Steps and inspect two list pages. Verify newest-first ordering, no duplicated boundary item, the correct compact comparison state, and navigation to a saved detail view.

### Tests for User Story 2 - Write and Run Red First

- [ ] T065 [P] [US2] Write a failing keyset token encode/decode and exact `created_at DESC, id DESC` stable-order unit test in `backend/tests/unit/domain/test_evolution_step_pagination.py`; 対応: US2; 依存: T008-T020, T064; 検証: before T071, `cd backend && uv run pytest tests/unit/domain/test_evolution_step_pagination.py` fails for absent pagination helpers and tie-break behavior.
- [ ] T066 [P] [US2] Write a failing compact comparison-summary state and reason-code unit test in `backend/tests/unit/domain/test_comparison_summary.py`; 対応: US2; 依存: T008-T020, T064; 検証: before T072, `cd backend && uv run pytest tests/unit/domain/test_comparison_summary.py` fails for missing summary logic.
- [ ] T067 [P] [US2] Write a failing 20-item keyset-list integration test with equal `created_at` values ordered by ID descending, a page boundary inside the equal-time group, no duplicate boundary item, and a new leading row inserted after the first-page read in `backend/tests/integration/test_evolution_step_list.py`; 対応: US2; 依存: T008-T020, T064; 検証: before T073-T075, `cd backend && uv run pytest tests/integration/test_evolution_step_list.py` fails for missing list behavior and exact continuation ordering; once green, continuing with the pre-insertion token excludes the new row without duplicating the boundary item, while a fresh request without a token includes the new row on the first page.
- [ ] T068 [P] [US2] Write a failing list endpoint contract test for summary fields, opaque tokens, malformed-token `422` behavior, and a structural assertion that the list path operation is not a coroutine function in `backend/tests/contract/test_evolution_step_list_contract.py`; 対応: US2; 依存: T008-T020, T064; 検証: before T075, `cd backend && uv run pytest tests/contract/test_evolution_step_list_contract.py` reaches response and call-style assertions and fails for the incomplete GET route.
- [ ] T069 [P] [US2] Write failing loading, empty, paged, fresh-mount reset, and detail-navigation tests in `frontend/src/features/evolution-steps/EvolutionStepListPage.test.tsx`; assert every OpenAPI `EvolutionStepListItem` displays ID, purpose, hypothesis, parent/result Run summaries or unset states, created/updated times, and comparison availability/reason, Parameter change count, best accuracies/delta, and Dataset change status; 対応: US2; 依存: T006, T064; 検証: before T076, T078-T079, `cd frontend && npm test -- --run src/features/evolution-steps/EvolutionStepListPage.test.tsx` reaches and fails the missing list-field and state-reset assertions, distinguishes unchanged from unavailable summaries, and proves that remounting or re-entering the list discards accumulated items and the prior token, then requests the first page without `pageToken` and displays a newly inserted leading Step.
- [ ] T070 [P] [US2] Write a failing successful-sync, pending-sync, and retained-detail warning-state test in `frontend/src/features/evolution-steps/RunSyncStatus.test.tsx`; 対応: US2; 依存: T006, T064; 検証: before T077, `cd frontend && npm test -- --run src/features/evolution-steps/RunSyncStatus.test.tsx` fails because reusable sync feedback is absent.

### Implementation for User Story 2

- [ ] T071 [P] [US2] Implement opaque `(created_at, id)` cursor encoding, validation, and keyset predicates in `backend/app/domain/pagination.py`; 対応: US2; 依存: T065; 検証: `cd backend && uv run pytest tests/unit/domain/test_evolution_step_pagination.py` passes for valid and malformed tokens.
- [ ] T072 [P] [US2] Implement compact availability, Parameter-count, accuracy, and Dataset summary calculation in `backend/app/domain/comparison.py`; 対応: US2; 依存: T066; 検証: `cd backend && uv run pytest tests/unit/domain/test_comparison_summary.py` passes with null reasons only for available states.
- [ ] T073 [US2] Add 21-row keyset reads using exact `created_at DESC, id DESC` order and summary-ready Snapshot joins to `backend/app/infrastructure/evolution_step_repository.py`; 対応: US2; 依存: T042, T043, T071, T072, T067; 検証: repository queries return at most 20 records, use ID descending for equal timestamps, avoid a duplicate boundary row, and emit a next token only when the twenty-first row exists.
- [ ] T074 [US2] Implement Evolution Step list orchestration and list-item mapping in `backend/app/services/evolution_step_list_service.py`; 対応: US2; 依存: T072, T073, T067; 検証: the integration list test gets deterministic ordering and summary objects from stored data only.
- [ ] T075 [US2] Add the token-paginated GET `/evolution-steps` route as a normal-`def` path operation because it invokes a synchronous database-backed service in `backend/app/api/routes/evolution_steps.py`; 対応: US2; 依存: T068, T074; 検証: `cd backend && uv run pytest tests/integration/test_evolution_step_list.py tests/contract/test_evolution_step_list_contract.py` passes, including the non-coroutine structure assertion.
- [ ] T076 [US2] Add typed paginated list retrieval and page-token types in `frontend/src/api/evolutionSteps.ts`; 対応: US2; 依存: T054, T068, T075; 検証: mocked list requests retain the server-issued token without exposing it as a text input.
- [ ] T077 [P] [US2] Implement compact Run synchronization progress, success, pending, and non-blocking failure feedback in `frontend/src/features/evolution-steps/RunSyncStatus.tsx`; 対応: US2; 依存: T070, T062; 検証: `cd frontend && npm test -- --run src/features/evolution-steps/RunSyncStatus.test.tsx` passes.
- [ ] T078 [US2] Implement loading, empty, next-page, selected-Step, and page-lifetime state behavior in `frontend/src/features/evolution-steps/EvolutionStepListPage.tsx`, retaining the accumulated items and server-issued token only for the current list-page mount and starting each remount or re-entry with empty items, no token, and a fresh first-page request; render ID, purpose, hypothesis, parent/result Run summaries or unset states, created/updated times, and compact comparison availability/reason, Parameter change count, best accuracies/delta, and Dataset change status; 対応: US2; 依存: T069, T076; 検証: `cd frontend && npm test -- --run src/features/evolution-steps/EvolutionStepListPage.test.tsx` passes for every required list field, distinct unchanged/unavailable displays, empty data, multiple pages without duplicated rows, and a fresh first page containing a newly inserted leading Step after remount or re-entry.
- [ ] T079 [US2] Wire the list page and reusable synchronization status into application routes and detail composition in `frontend/src/pages/EvolutionStepListPage.tsx` and `frontend/src/pages/EvolutionStepDetailPage.tsx`; 対応: US2; 依存: T062, T077, T078; 検証: `cd frontend && npm test -- --run src/features/evolution-steps/EvolutionStepListPage.test.tsx src/features/evolution-steps/RunSyncStatus.test.tsx` passes.

**Checkpoint**: US2 can be tested from seeded local database state; a failed MLflow synchronization never prevents list or saved-detail inspection.

---

## Phase 5: User Story 3 - 派生元との差分を振り返る (Priority: P3)

**Goal**: Return and display only meaningful Parameter, accuracy, and Dataset Input differences from finalized Snapshots with explicit unavailable reasons.

**Independent Test**: Seed finalized parent and result Snapshots with changed Parameters, repeated Metrics, and Dataset Inputs. Confirm the comparison shows additions, changes, removals, percentage-formatted accuracy delta, and only valid Dataset difference states.

### Tests for User Story 3 - Write and Run Red First

- [ ] T080 [P] [US3] Write failing Parameter and Dataset Input difference tests, including ambiguous pairing and no-row-data cases, in `backend/tests/unit/domain/test_parameter_and_dataset_differences.py`; 対応: US3; 依存: T008-T020, T079; 検証: before T085, `cd backend && uv run pytest tests/unit/domain/test_parameter_and_dataset_differences.py` fails for missing difference rules.
- [ ] T081 [P] [US3] Write failing comparison availability and unavailable-reason precedence tests in `backend/tests/unit/domain/test_comparison_availability.py`; 対応: US3; 依存: T008-T020, T079; 検証: before T086, `cd backend && uv run pytest tests/unit/domain/test_comparison_availability.py` fails for incomplete comparison state mapping.
- [ ] T082 [P] [US3] Write a failing finalized-Snapshot integration test for two-Run Parameter, accuracy, and Dataset comparison reads, including comparison stability after synchronizing only mutable Run Reference metadata, in `backend/tests/integration/test_snapshot_comparison.py`; 対応: US3; 依存: T008-T020, T079; 検証: before T085, T086, T087, and T088, `cd backend && uv run pytest tests/integration/test_snapshot_comparison.py` fails for missing comparison services and endpoints; once green, changing a finalized Run's current name, status, execution times, and `lastSyncedAt` through synchronization leaves the comparison result identical because it is derived from immutable Snapshots. Single-Run best-step Metric reads are already verified in US1.
- [ ] T083 [P] [US3] Write failing Comparison OpenAPI contract tests, including a structural assertion that the comparison path operation is not a coroutine function, in `backend/tests/contract/test_comparison_contract.py`; 対応: US3; 依存: T008-T020, T079; 検証: before T087-T088, `cd backend && uv run pytest tests/contract/test_comparison_contract.py` reaches response and call-style assertions and fails for the incomplete comparison route; BestStepMetrics contract coverage remains in the US1 suite.
- [ ] T084 [P] [US3] Write failing available, unavailable, percentage-format, and Dataset-status rendering tests in `frontend/src/features/evolution-steps/ComparisonPanel.test.tsx`, plus comparison-loading and independent-error tests in `frontend/src/features/evolution-steps/EvolutionStepDetailPage.test.tsx`; 対応: US3; 依存: T006, T079; 検証: before T089, T090, and T091, `cd frontend && npm test -- --run src/features/evolution-steps/ComparisonPanel.test.tsx src/features/evolution-steps/EvolutionStepDetailPage.test.tsx` fails for missing comparison behavior while existing US1 single-Run display cases remain green.

### Implementation for User Story 3

- [ ] T085 [US3] Implement Parameter union and Dataset Input pairing/difference functions in `backend/app/domain/differences.py`; 対応: US3; 依存: T080; 検証: `cd backend && uv run pytest tests/unit/domain/test_parameter_and_dataset_differences.py` passes, including `changed`, `parent_only`, `result_only`, and unavailable cases.
- [ ] T086 [US3] Implement full comparison assembly, fixed unavailable-reason precedence, and summary reuse in `backend/app/services/comparison_service.py`; 対応: US3; 依存: T072, T081, T085; 検証: `cd backend && uv run pytest tests/unit/domain/test_comparison_availability.py` passes with mutually exclusive status/reason fields.
- [ ] T087 [US3] Define Comparison, accuracy, and Dataset response schemas in `backend/app/api/schemas/comparison.py`; 対応: US3; 依存: T083, T086; 検証: schema serialization preserves ratios, explicit `null`s, and allowed comparison reason enums; it does not redefine the US1 BestStepMetrics schema.
- [ ] T088 [P] [US3] Add normal-`def` GET `/evolution-steps/{evolutionStepId}/comparison` because it invokes a synchronous database-backed service in `backend/app/api/routes/evolution_steps.py`; 対応: US3; 依存: T086, T087; 検証: comparison portions of `cd backend && uv run pytest tests/integration/test_snapshot_comparison.py tests/contract/test_comparison_contract.py` pass, including the non-coroutine structure assertion.
- [ ] T089 [P] [US3] Add typed comparison retrieval and response types in `frontend/src/api/evolutionSteps.ts`; 対応: US3; 依存: T083, T088; 検証: mock responses distinguish unavailable comparison, accuracy, and Dataset reasons.
- [ ] T090 [US3] Implement Parameter, accuracy, and Dataset difference rendering in `frontend/src/features/evolution-steps/ComparisonPanel.tsx`; 対応: US3; 依存: T084, T089; 検証: `cd frontend && npm test -- --run src/features/evolution-steps/ComparisonPanel.test.tsx` passes without calling Dataset metadata changes row additions or deletions.
- [ ] T091 [US3] Integrate two-Run comparison loading and display into `frontend/src/pages/EvolutionStepDetailPage.tsx`, preserving the existing US1 per-Run best-step Metric loads; 対応: US3; 依存: T062, T084, T090; 検証: `cd frontend && npm test -- --run src/features/evolution-steps/ComparisonPanel.test.tsx src/features/evolution-steps/EvolutionStepDetailPage.test.tsx` passes, including result-only Metric viewing and independent comparison failures.

**Checkpoint**: US3 reads immutable Snapshots only and makes partial comparison availability explicit instead of representing unavailable data as unchanged.

---

## Phase 6: User Story 4 - Lineageを確認する (Priority: P4)

**Goal**: Reconstruct current Lineage around a selected Evolution Step and display ordered ancestors, descendants, branches, and upstream boundaries.

**Independent Test**: Seed a branched multi-generation set of current Run links, select a middle Step, and confirm nearest-first ancestors, breadth-first descendants ordered by Step ID, distances, and parent-Step identifiers.

### Tests for User Story 4 - Write and Run Red First

- [ ] T092 [P] [US4] Write failing nearest-first ancestor, breadth-first descendant, branch-order, shared-parent fan-out, and upstream-boundary unit tests in `backend/tests/unit/domain/test_lineage_traversal.py`; 対応: US4; 依存: T008-T020, T091; 検証: before T096, `cd backend && uv run pytest tests/unit/domain/test_lineage_traversal.py` fails for missing traversal behavior.
- [ ] T093 [P] [US4] Write a failing current-links-only multi-generation Lineage integration test including multiple child Steps that share the same parent Run and Lineage-topology stability after synchronizing only mutable Run Reference metadata in `backend/tests/integration/test_lineage.py`; 対応: US4; 依存: T008-T020, T091; 検証: before T097-T100, `cd backend && uv run pytest tests/integration/test_lineage.py` fails for missing persisted graph reads and confirms shared-parent fan-out; once green, synchronization may update embedded Run names, statuses, execution times, and `lastSyncedAt`, but the selected/ancestor/descendant Evolution Step IDs, `parentEvolutionStepId` values, distances, branches, and ordering remain identical because current Run links were not mutated.
- [ ] T094 [P] [US4] Write a failing Lineage endpoint contract test for distances, parent Evolution Step IDs, boundary Run representation, and a structural assertion that the Lineage path operation is not a coroutine function in `backend/tests/contract/test_lineage_contract.py`; 対応: US4; 依存: T008-T020, T091; 検証: before T099-T100, `cd backend && uv run pytest tests/contract/test_lineage_contract.py` reaches response and call-style assertions and fails for the absent endpoint.
- [ ] T095 [P] [US4] Write failing ancestor, descendant, shared-parent branch, root, and external-boundary rendering tests in `frontend/src/features/evolution-steps/LineagePanel.test.tsx`, plus a returning-from-edit current-Lineage reload test in `frontend/src/features/evolution-steps/EvolutionStepDetailPage.test.tsx`; 対応: US4; 依存: T006, T091; 検証: before T101-T103, `cd frontend && npm test -- --run src/features/evolution-steps/LineagePanel.test.tsx src/features/evolution-steps/EvolutionStepDetailPage.test.tsx` fails because the Lineage panel and reload behavior are absent.

### Implementation for User Story 4

- [ ] T096 [US4] Extend the shared graph rules with deterministic ancestor and descendant traversal functions in `backend/app/domain/lineage.py`; 対応: US4; 依存: T038, T092; 検証: `cd backend && uv run pytest tests/unit/domain/test_lineage_traversal.py` passes for all order and boundary cases.
- [ ] T097 [P] [US4] Add current-link ancestor and descendant lookup queries in `backend/app/infrastructure/evolution_step_repository.py`; 対応: US4; 依存: T042, T093; 検証: queries exclude stale history links and return enough data to identify direct parent Steps.
- [ ] T098 [US4] Implement selected-centered Lineage assembly in `backend/app/services/lineage_service.py`; 対応: US4; 依存: T096, T097, T093; 検証: `cd backend && uv run pytest tests/integration/test_lineage.py` passes for branched and external-boundary graphs.
- [ ] T099 [US4] Define selected, ancestor, and descendant Lineage response schemas in `backend/app/api/schemas/lineage.py`; 対応: US4; 依存: T094, T098; 検証: response schemas require `distanceFromSelected` and `parentEvolutionStepId` for every returned Step.
- [ ] T100 [US4] Add normal-`def` GET `/evolution-steps/{evolutionStepId}/lineage` because it invokes a synchronous database-backed service in `backend/app/api/routes/evolution_steps.py`; 対応: US4; 依存: T094, T098, T099; 検証: `cd backend && uv run pytest tests/integration/test_lineage.py tests/contract/test_lineage_contract.py` passes, including the non-coroutine structure assertion.
- [ ] T101 [US4] Add typed Lineage retrieval and response types in `frontend/src/api/evolutionSteps.ts`; 対応: US4; 依存: T094, T100; 検証: mock requests preserve ordered arrays and nullable boundary fields.
- [ ] T102 [US4] Implement the selected-centered ancestor and descendant display in `frontend/src/features/evolution-steps/LineagePanel.tsx`; 対応: US4; 依存: T095, T101; 検証: `cd frontend && npm test -- --run src/features/evolution-steps/LineagePanel.test.tsx` passes for root, branch, and external-boundary states.
- [ ] T103 [US4] Add Lineage loading and display to `frontend/src/pages/EvolutionStepDetailPage.tsx`; 対応: US4; 依存: T091, T102; 検証: `cd frontend && npm test -- --run src/features/evolution-steps/LineagePanel.test.tsx src/features/evolution-steps/EvolutionStepDetailPage.test.tsx` passes, comparison and Lineage failures remain independent, and returning from an edit reloads Lineage from current links without suppressing saved detail.

**Checkpoint**: US4 reconstructs the current graph without a stored Lineage table and remains independently verifiable from seeded current links.

---

## Phase 7: Overall Integration and Cross-Cutting Verification

**Purpose**: Confirm the documented HTTP boundary, critical browser journey, performance constraints, and reproducible operating instructions across all completed stories.

- [ ] T104 [P] Write and run an OpenAPI response-conformance suite against every implemented route in `backend/tests/contract/test_openapi_conformance.py`; 対応: 全体統合 (US1-US4); 依存: T053, T075, T088, T052, T100; 検証: `cd backend && uv run pytest tests/contract/test_openapi_conformance.py` initially exposes any contract drift, then passes against `specs/001-experiment-evolution/contracts/openapi.yaml`.
- [ ] T105 [P] Configure the Playwright E2E execution boundary by adding locked `@playwright/test` development dependency entries and `scripts.test:e2e = "playwright test"` in `frontend/package.json` and `frontend/package-lock.json`, and creating `frontend/playwright.config.ts` with `testDir: "./e2e"`, one Chromium project, Vite `baseURL` at `http://127.0.0.1:5173`, explicit FastAPI and Vite `webServer` entries, and `retain-on-failure` trace plus `only-on-failure` screenshot settings; install the matching Chromium binary with `npx playwright install chromium`; 対応: 全体統合 (US1-US4); 依存: T002, T063, T079, T091, T103; 検証: `cd frontend && npm ci && npm pkg get 'scripts.test:e2e' && npx playwright install chromium && npm run test:e2e -- --list --pass-with-no-tests --project=chromium` loads the configuration and recognizes the Chromium project before T106 adds the flow, without running a browser test or starting an unmanaged server.
- [ ] T106 Create the critical create-and-attach browser flow with one visible validation failure in `frontend/e2e/create-and-attach.spec.ts`; 対応: 全体統合 (US1-US4); 依存: T105, T063, T103; 検証: `cd frontend && npm run test:e2e -- create-and-attach` creates a Step through browser requests to the Vite origin's relative `/api/v1` path, reaches FastAPI through the configured proxy, attaches a Run, verifies its detail, and retains trace/screenshot artifacts on failure.
- [ ] T107 [P] Add seeded read-performance checks for list, comparison, detail, and 100-Step Lineage budgets in `backend/tests/integration/test_read_performance.py`; 対応: 全体統合 (US1-US4); 依存: T075, T086, T098; 検証: against exactly 1,000 seeded Evolution Steps including a connected 100-Step Lineage, `cd backend && uv run pytest tests/integration/test_read_performance.py` performs 5 unmeasured warm-ups and 30 measured requests per target, excludes fixture setup and MLflow synchronization, calculates nearest-rank p95, and verifies the stated 500 ms and 1 s targets.
- [ ] T108 Update actual setup, migration, test, MLflow fixture, Playwright Chromium installation, failure-triage, and timed manual-acceptance procedures in `README.md` and `specs/001-experiment-evolution/quickstart.md`; 対応: 全体統合 (US1-US4); 依存: T104-T107; 検証: a clean environment can follow both documents to start MySQL, migrate, install the matching Chromium binary, run backend/frontend tests, execute the E2E flow, and identify the start/end conditions for SC-001 through SC-004 without undocumented credentials.
- [ ] T109 Execute the final backend, frontend, E2E, lint, and quickstart verification matrix recorded in `specs/001-experiment-evolution/quickstart.md`; 対応: 全体統合 (US1-US4); 依存: T104-T108; 検証: `cd backend && uv run pytest && uv run ruff check .`, `cd frontend && npm test -- --run && npm run build && npm run test:e2e`, and the documented quickstart scenario all pass with results attached to the implementation review.
- [ ] T110 Execute and record timed user acceptance for SC-001 through SC-004 in `specs/001-experiment-evolution/validation-results.md`, recording for each criterion the execution date, tested commit/ref, environment and test dataset, measurement start/end conditions, measured result, pass/fail judgment, and evidence link or diagnostic notes; 対応: 全体統合 (US1-US4); 依存: T109; 検証: SC-001 is timed from display of an empty create form until the created detail shows the selected result Run, SC-002 from opening a captured detail until the tester identifies Parameter differences, best accuracy, and its delta, and SC-003 from opening a prepared multi-generation Lineage until every expected ancestor, descendant, and order is identified; each stays within its 3-minute or 1-minute limit, and SC-004 records 100% expected-field and reconstructed-Lineage agreement after stopping and restarting the application while retaining the same MySQL data, with every result traceable to the tested build and retained as implementation-review evidence.

---

## Dependencies and Execution Order

### Phase Dependencies

- Phase 1 Setup has no prerequisites.
- Phase 2 Foundational depends on Phase 1 and blocks all product behavior.
- US1 depends on Phase 2 and is the MVP implementation increment, including single-Run Parameters, Dataset Inputs, and the complete best-step Metrics read/display path; it has no dependency on the US3 comparison endpoint.
- US2 depends on US1 because it lists and summarizes persisted Evolution Steps.
- US3 depends on US2's shared compact comparison calculation and US1's captured Snapshots.
- US4 follows US3 in the initial implementation sequence because both integrate with `EvolutionStepDetailPage.tsx`; this avoids overlapping edits while preserving US1's current links as the Lineage data source.
- Phase 7 depends on all selected story checkpoints.

### User Story Dependency Graph

```text
Setup -> Foundational -> US1 -> US2 -> US3 -> US4 -> Overall Integration
```

### Parallel Opportunities

- Setup: T001, T002, T003, and T004 have no mutual file conflicts; T003 and T004 are explicitly parallel.
- Foundational: T008, T010, T012, and T019 are parallel candidates after their named dependencies. T016 follows T010 because both edit `backend/tests/conftest.py`; it is not marked `[P]`. Each implementation follows its named red test.
- US1: T021 through T035 (in the US1 test section) are parallel red tests in separate files. T036-T043 are independently implementable where their named dependencies permit. T044 then T045, T051 then T052, and T055 then T056 are ordered because each pair edits the same file. T048/T049, T050/T051, and T054/T055 can proceed in parallel after their dependencies.
- US2: T065-T070 are parallel red tests, followed by T071/T072 and T077 where dependencies permit.
- US3: T080-T084 are parallel red tests in distinct files; comparison route T088 precedes comparison client T089. Single-Run best-step Metrics were completed in US1.
- US4: T092-T095 are parallel red tests; T097 can be implemented alongside T096 after their independent tests are written.
- Integration: T104, T105, and T107 can start in parallel after the relevant story checkpoints; T106 follows T105, and final manual acceptance T110 follows the complete verification matrix T109.

## Parallel Execution Examples

### User Story 1

```text
Task: T021 through T035 - write all US1 red tests, including local best-step Metrics, in separate files
Task: T036 - backend/app/domain/evolution_step.py
Task: T037 - backend/app/domain/run_snapshot.py
Task: T038 - backend/app/domain/lineage.py
Task: T039 - backend/app/infrastructure/mlflow_gateway.py
```

### User Story 2

```text
Task: T065 through T070 - write pagination, summary, API, and UI red tests
Task: T071 - backend/app/domain/pagination.py
Task: T072 - backend/app/domain/comparison.py
Task: T077 - frontend/src/features/evolution-steps/RunSyncStatus.tsx
```

### User Story 3

```text
Task: T080 through T084 - write comparison domain, integration, contract, and UI red tests
Task: T088 - comparison API route (after its dependencies)
Task: T089 - Evolution Step comparison client (after T088)
```

### User Story 4

```text
Task: T092 through T095 - write traversal, integration, contract, and UI red tests
Task: T096 - backend/app/domain/lineage.py traversal logic
Task: T097 - backend/app/infrastructure/evolution_step_repository.py graph reads
```

## Implementation Strategy

### MVP First

1. Complete Phase 1 and Phase 2, including their verification commands.
2. Complete T021-T064 as a red-to-green vertical slice for US1.
3. Run the US1 unit, integration, contract, and component checks before treating the MVP as reviewable.
4. Demonstrate creation, attachment, synchronization, all Parameters/Dataset Inputs, and lazy best-step Metric viewing for a result-only Run with the MLflow fake before beginning broader views.

### Incremental Delivery

1. Deliver US1 for durable Evolution Step creation, Run attachment, and each Run's saved information and best-step Metrics.
2. Add US2 for browsing and finding stored work with stable pagination.
3. Add US3 for evidence-based comparisons from immutable Snapshots.
4. Add US4 for current-link Lineage tracing.
5. Run Phase 7 once all desired stories are integrated.

### Test-First Completion Rule

- Every task in a `Tests for User Story` subsection is written and executed red before its dependent implementation task starts.
- Each implementation task is complete only when its named focused verification passes.
- A story checkpoint requires its unit, integration, contract, and applicable frontend tests to pass together.

## Format Validation

All 110 tasks use the required `- [ ] T### [P?] [US?] Description` checklist format. User-story tasks carry exactly one `[US1]` through `[US4]` label; setup, foundational, and integration tasks use no user-story label. Every task includes target paths, a corresponding story scope, explicit dependencies, and a verification method.
